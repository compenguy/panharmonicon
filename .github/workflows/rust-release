name: Rust Release

on:
  push:
    tags: [ v0.2*, v1.* ]

jobs:
  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
    - uses: actions-rs/cargo@v1
    - name: Release
      run: cargo build --release --all-features
